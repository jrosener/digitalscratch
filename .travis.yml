language: cpp
compiler: gcc
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq build-essential qt-sdk
script:
  - cd libdigitalscratch
  - qmake CONFIG+=test
  - make
  - ./libdigitalscratch-test
